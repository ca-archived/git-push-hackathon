import UIKit

enum Color {
    
    enum Background {
        static let black = UIColor.black
    }
    
    enum Font {
        static let gray = UIColor.gray
        static let white = UIColor.white
    }
}
