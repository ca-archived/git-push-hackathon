import * as React from "react";
import { Message } from "../components/templates/message";

export const Settings: React.StatelessComponent = _ => {
  return <Message>Settings page is under construction.</Message>;
};
