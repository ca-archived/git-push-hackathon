import * as React from "react";
import { Message } from "../components/templates/message";

export const Account: React.StatelessComponent = _ => {
  return <Message>Account page is under construction.</Message>;
};
